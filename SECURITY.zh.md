# 安全策略

[English](./SECURITY.md) | [简体中文](./SECURITY.zh.md)

> 本插件按设计会把文件内容写到磁盘上。请把它的快照库当作敏感数据。

## 支持的版本

| 版本 | 支持状态 |
| --- | --- |
| `0.1.1` | 支持 |
| `< 0.1.1` | 不支持——请升级 |

## 报告漏洞

- 在本仓库开一条私密的安全公告(GitHub security advisory)。
- 写清插件版本、dsh 版本,以及一段脱敏后的账本片段。
- 绝不把真实凭据或密钥材料贴进报告。
- 这是一个业余项目,首次回复可能要几天。

## 数据处置

- 文件名命中 `secretNames` 的文件**永不**入库内容:只记录「它被改过」这一事实。
- 内容是**未压缩、未加密**存储的——因为恢复必须在别的工具都读不了盘时仍然可用。
- 快照库在工作区之外,位于 `$DSH_HOME/workspace-rewind/`,并被排除在自己的捕获之外。
- 恢复计划对「内容未知」的路径保持原样;既不猜历史,也不删除它们。

## 不算漏洞

- 以「本来就能读工作区文件」的同一个用户身份读取快照库。
- 恢复计划对密钥样式或超限文件报告「保持现状」。
- 卸载插件后记录仍在——`uninstall.sh` 会保留快照库,直到你显式 `--purge --yes`。
